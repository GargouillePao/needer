var app = require("../src/index")();
app.config({
    port:{
        http:8000,
        ws:8080
    }
});
app.config({
    path:{
        view:"view",
        model:"models",
        controller:{
            root:"controller",
            ws:"sockets"
        }
    }
});
app.config({
    template:"ejs"
});
app.use({
    router:{src:"router",url:"/"}
});
app.use({
    ws:{src:"socket",nsp:""}
});
app.start();